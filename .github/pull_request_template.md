**What and why**

**Testing**

- [ ] `app/server/.venv/bin/python -m pytest tests -q` passes
- [ ] `cd app/web && npm run build` passes (if web changed)
- [ ] No test touches the network or real credentials
- [ ] Docs updated (if behavior or setup changed)
- [ ] CHANGELOG.md "Unreleased" line (if users would notice)
